# Every credential lives in this directory and must never be committed.
# The directory itself is tracked so that the project structure is
# visible in the repository; its contents are not.
*
!.gitignore
